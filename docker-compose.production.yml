services:
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/cloudflare/cert.pem:/etc/cloudflare/cert.pem:ro
      - /etc/cloudflare/key.pem:/etc/cloudflare/key.pem:ro
    depends_on:
      - web
    networks:
      - web

  web:
    image: ghcr.io/carloslimamecwide/anita:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    networks:
      - web

networks:
  web:
